//将所有地指令导入，导出
import Focus from "./focus";

export default (app)=>{
    Focus(app)
}
//在main.js中
// import App from './App.vue'
// const App = createApp(App)
// import directive from '路径'
// directive(app)
// 然后就可以直接在组件中使用v-name,name为自己定义的名字